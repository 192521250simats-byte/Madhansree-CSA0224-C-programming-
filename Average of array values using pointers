#include <stdio.h>

int main() {
    int a[5] = {10, 20, 30, 40, 50};
    int sum = 0;
    float avg;
    int *p = a;

    for(int i = 0; i < 5; i++) {
        sum += *(p + i);
    }

    avg = sum / 5.0;
    printf("Average = %.2f", avg);

    return 0;
}
