#include <stdio.h>

int main() {
    int a, square, cube;
    printf("square and cube of a number");
    scanf("%d", &a);
    square = a * a;
    cube = a * a * a;
    printf("square-%d\n", square);
    printf("cube-%d\n", cube);
    return 0;
}
